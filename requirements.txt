# 基于Python的简易端口扫描器依赖列表

# Web界面依赖
Flask>=2.0.0

# 打包工具依赖（可选）
PyInstaller>=5.0.0

# 核心依赖（主要使用Python标准库，无需额外安装）
# 以下为可选依赖，用于增强功能

# 开发依赖（可选）
# pytest>=7.0.0  # 用于单元测试
# black>=22.0.0  # 代码格式化
# flake8>=5.0.0  # 代码风格检查
# mypy>=0.991    # 类型检查

# 性能监控（可选）
# psutil>=5.9.0  # 系统和进程监控

# 网络工具（可选）
# scapy>=2.5.0   # 高级网络扫描（需要管理员权限）

# 注意事项：
# 1. 本项目主要依赖Python标准库，无需安装额外包即可运行命令行版本
# 2. Web界面需要安装Flask
# 3. 打包成exe需要安装PyInstaller
# 4. 在Windows上，某些网络操作可能需要管理员权限
# 5. 在Linux/macOS上，某些扫描类型可能需要root权限