# 基于Python的简易端口扫描器依赖列表
# 核心依赖（主要使用Python标准库，无需额外安装）

# 可选依赖（用于增强功能）
# 如果需要异步扫描支持，可以安装以下包：
# asyncio  # Python 3.4+ 标准库内置

# 如果需要更好的IP地址处理：
# ipaddress  # Python 3.3+ 标准库内置

# 如果需要JSON美化输出：
# json  # 标准库内置

# 如果需要CSV处理：
# csv  # 标准库内置

# 如果需要HTML报告生成：
# 无额外依赖，使用标准库

# 开发依赖（可选）
# pytest>=7.0.0  # 用于单元测试
# black>=22.0.0  # 代码格式化
# flake8>=5.0.0  # 代码风格检查
# mypy>=0.991    # 类型检查

# 性能监控（可选）
# psutil>=5.9.0  # 系统和进程监控

# 网络工具（可选）
# scapy>=2.5.0   # 高级网络扫描（需要管理员权限）

# 注意事项：
# 1. 本项目主要依赖Python标准库，无需安装额外包即可运行
# 2. 上述可选依赖可根据需要选择安装
# 3. 在Windows上，某些网络操作可能需要管理员权限
# 4. 在Linux/macOS上，某些扫描类型可能需要root权限